{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .spago",
    "build": "eslint src && spago build -- --censor-lib --strict"
  },
  "devDependencies": {
    "eslint": "^8.10.0",
    "purescript-psa": "^0.8.0",
    "rimraf": "^3.0.2",
    "spago": "^0.20.7"
  }
}
